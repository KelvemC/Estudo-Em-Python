a = 8
b = 3
c = 2
"""
if b > a: print("b é maior que a")
print("codigo fora do if")
"""

"""
Como encurtar o if e else

Em python a forma de encurtar o if e else é a mesma forma de representar
um operador ternário em python, não existe um operador ternário
como em outras linguagens de programação: java, c, php.

Em python o if e else é o próprio operador ternário, porém qual
if e else? o if e else que é na sua forma mais curta.  
print("B") if b < a else print("A") #Operador ternário em python, expressões condicionais
"""

if a > b:
    if a > c:
        print("A é maior que b e que c")